Accepted:
- Added `app/llms.txt/route.ts`.
- The route uses `getRepoSections()` so future canonical project additions are included automatically.
- Host detection sets `Projects` for `projects.micr.dev` and `Proyectos` for `proyectos.micr.dev`.

Rejected:
- Per-domain duplicated files.

Conflicts:
- None.

Decisions:
- Include a portfolio URL for every entry and a separate project link field for live/public external links.
